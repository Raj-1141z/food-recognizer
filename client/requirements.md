## Packages
framer-motion | Smooth animations for UI elements
react-dropzone | robust drag-and-drop file upload handling
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without conflicts

## Notes
Backend expects base64 string in `image` field for POST /api/scan
Backend returns { foodName: string, analysis: any }
